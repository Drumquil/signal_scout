# Stage 03: Implementation

## Goal

Make the scoped change while preserving existing runtime behaviour.

## Inputs

- Layer 3: `../../_config/change-safety.md`
- Layer 4: approved design scope from stage 02
- Layer 4: the exact files to edit

## Process

- Edit only the files required for the task.
- Keep changes readable and reversible.
- When adding structure, prefer scaffolding files and docs over moving code.
- If a path changes, update every known reference before validation begins.

## Outputs

- Updated source, docs, or workspace scaffolding
- Optional implementation notes in `output/`
