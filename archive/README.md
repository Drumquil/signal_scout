# Archive

This folder holds historical or superseded project markdown that is useful for
traceability but should not be part of the default Codex read path.

Use it only when you need:

- older decision context
- historical validation detail
- schema evolution history
- prior handover language

Default starting docs live in `docs/`.
